server.port=${PORT:8080} 
#spring.datasource.url=jdbc:postgresql://localhost:5432/vaidh
#spring.datasource.username=postgres
#spring.datasource.password=123

spring.datasource.url=jdbc:postgresql://${PSQL_DB_SERVER}:5432/${PSQL_DB_NAME}
spring.datasource.username=${PSQL_DB_USERNAME}
spring.datasource.password=${PSQL_DB_PW}
spring.jpa.show-sql=true
spring.jpa.generate-ddl=true
spring.jpa.hibernate.ddl-auto=none
spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true

jwt.secret=${JWT_SECRET}

firebase.db.url=${FIREBASE_URL}
vaidh.mail.password=${VAIDH_MAIL_PW}
vaidh.email=${VAIDH_MAIL_ID}

#p/w gmail : vaidh.notify :: Vaidh123